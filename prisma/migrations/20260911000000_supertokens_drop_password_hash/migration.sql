-- ЛР7 (замена собственного JWT на SuperTokens): хранение и проверка пароля
-- целиком переходит к SuperTokens Core (собственная БД провайдера), поэтому
-- локальный хеш пароля в нашей схеме больше не нужен и не заполняется.
ALTER TABLE "User" DROP COLUMN "passwordHash";
